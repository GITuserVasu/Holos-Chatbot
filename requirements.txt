# ---------------------------------------------
# 🌾 Holos Agri Assistant – Python Dependencies
# ---------------------------------------------

# Load environment variables (e.g., API keys, model names) from a .env file
python-dotenv==1.1.1

# Framework for building RESTful backend APIs
fastapi==0.115.14

# ASGI server used to run FastAPI applications
uvicorn==0.29.0

# Data validation and serialization library (used for request/response models)
pydantic==2.8.2

# Helps manage app configurations using environment variables with Pydantic
pydantic-settings==2.11.0

# Core LangChain library – connects LLMs, tools, and custom logic
langchain==0.2.17

# LangChain wrapper for OpenAI – integrates GPT models for responses and embeddings
langchain-openai==0.1.25

# Community integrations for LangChain (document loaders, retrievers, etc.)
langchain-community==0.2.19

# LangGraph – used to build multi-node pipelines like the RAG + CSM graph
langgraph==0.1.19

# Tokenizer for LLM text processing (used for embeddings and message length)
tiktoken==0.7.0

# FAISS – high-performance vector database for document similarity search
faiss-cpu==1.12.0

# DuckDB – in-memory analytical SQL engine (used for fast CSV queries)
duckdb==0.10.3

# Data manipulation and analysis (used for CSV summaries, stats, etc.)
pandas==2.2.3

# Apache Arrow – columnar memory format for efficient data handling with Pandas/DuckDB
pyarrow==15.0.2

# Structured logging library for better debugging and API event tracking
structlog==23.3.0

# Streamlit – frontend web app for user interaction (chat UI and context sidebar)
streamlit==1.35.0

# Additional dependencies that are being used
langchain-core==0.2.43
langchain-text-splitters==0.2.4
openai==1.109.1
numpy==1.26.4
SQLAlchemy==2.0.44
